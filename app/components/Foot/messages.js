/*
 * Foot Messages
 *
 * This contains all the text for the Foot component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.Foot';

export default defineMessages({
  footer: {
    id: `${scope}.footer`,
    defaultMessage: 'I am a footer',
  },
});
